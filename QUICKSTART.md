# 🚀 快速启动指南

> 哼，跟着本小姐的步骤，马上就能开始使用啦！(￣▽￣)／

## 📋 前置要求

在开始之前，请确保你已经安装了：

- **Node.js** (版本 18 或更高) - [下载地址](https://nodejs.org/)
- **npm** (通常随 Node.js 一起安装)
- **代码编辑器** (推荐 VS Code)

## 🔑 获取 AI API 密钥

本小姐推荐使用 **DeepSeek**，性价比超高！

### 方式1：DeepSeek（强烈推荐 ⭐⭐⭐⭐⭐）

1. 访问：https://platform.deepseek.com/
2. 注册账号
3. 在控制台获取 API Key
4. 充值（建议先充 10 元试试）

**价格：**
- 输入：¥0.001 / 千tokens
- 输出：¥0.002 / 千tokens
- **降重一篇5000字论文大约花费 ¥0.05-0.10**，超级便宜！

### 方式2：OpenAI

1. 访问：https://platform.openai.com/
2. 注册账号（需要国际信用卡）
3. 获取 API Key

### 方式3：其他

你也可以使用其他兼容 OpenAI API 格式的服务，比如：
- 阿里通义千问
- 智谱 AI
- 月之暗面（Kimi）

## 📦 安装步骤

### 1. 安装后端依赖

```bash
cd backend
npm install
```

### 2. 配置环境变量

在 `backend` 目录下创建 `.env` 文件：

```bash
# 复制示例配置
cp .env.example .env
```

然后编辑 `.env` 文件，填入你的 API 密钥：

```env
# DeepSeek API（推荐）
DEEPSEEK_API_KEY=你的密钥粘贴在这里
```

### 3. 启动后端服务

```bash
npm run dev
```

看到以下信息说明启动成功：

```
╔═══════════════════════════════════════════════════════╗
║                                                       ║
║   🎓 论文降重工具后端服务启动成功！                    ║
║                                                       ║
║   🚀 服务地址: http://localhost:3000                   ║
║   📝 健康检查: http://localhost:3000/health            ║
║                                                       ║
║   哼，本小姐的服务器已经准备好了！(￣▽￣)／             ║
║                                                       ║
╚═══════════════════════════════════════════════════════╝
```

### 4. 打开前端页面

在浏览器中访问：

```
http://localhost:3000
```

（因为后端配置了静态文件服务，直接访问后端地址就会显示前端页面）

## 🎯 使用方法

### 方式1：文本输入

1. 点击"文本输入"标签
2. 在文本框中粘贴需要降重的论文内容
3. 选择降重强度（轻度/中度/重度）
4. 点击"开始降重"按钮
5. 等待AI处理（通常需要几秒到几十秒）
6. 查看降重结果！

### 方式2：文件上传

1. 点击"文件上传"标签
2. 选择或拖拽文件（支持 .docx、.doc、.txt）
3. 选择降重强度
4. 点击"开始降重"
5. 等待处理完成
6. 查看并下载结果

### 💡 使用技巧

**降重强度选择：**
- **轻度** - 同义词替换，保持原有结构，适合微调
- **中度** - 句式重组，改变表达方式，**推荐使用**
- **重度** - 大幅改写，完全重新表达，适合高重复率内容

**分段处理：**
- 对于长篇论文，建议分段处理（每段 2000-3000 字）
- 这样效果更好，也能避免超时

**多次降重：**
- 如果第一次降重效果不满意，可以用结果再降重一次
- 但不建议超过3次，否则可能影响语义准确性

## 📊 查看历史记录

所有降重记录都会自动保存在浏览器本地存储中：

- 在右侧历史记录面板查看
- 点击任意记录可以查看详情
- 点击垃圾桶图标清空历史

## 🛠️ 常见问题

### Q1: 后端启动失败？

**检查端口是否被占用：**
```bash
# Windows
netstat -ano | findstr :3000

# Mac/Linux
lsof -i :3000
```

如果端口被占用，可以修改 `backend/.env` 中的 `PORT` 值。

### Q2: API 调用失败？

**检查：**
1. API 密钥是否正确
2. 账户是否有余额
3. 网络连接是否正常

**测试 API 连接：**
```bash
curl http://localhost:3000/api/health
```

### Q3: 文件上传失败？

**检查：**
1. 文件大小不超过 10MB
2. 文件格式为 .docx、.doc 或 .txt
3. 文件中没有太多图片、表格、公式

### Q4: 降重效果不好？

**建议：**
1. 尝试不同的降重强度
2. 分段处理后再组合
3. 手动调整明显不合理的地方
4. 使用"重度"模式重新降重

### Q5: 费用问题？

DeepSeek 的价格非常亲民：
- 一篇 5000 字的论文，中度降重大约花费 ¥0.05-0.10
- 充值 10 元可以降重约 100-200 篇论文
- 比起代写便宜太多了！

## 🎓 进阶功能

### 自定义前端端口

如果你想用其他端口运行前端，可以使用简单的 HTTP 服务器：

```bash
# 安装 http-server
npm install -g http-server

# 在 frontend 目录运行
cd frontend
http-server -p 5173
```

然后访问：http://localhost:5173

**注意：** 需要修改 `frontend/src/services/api.js` 中的 `API_BASE_URL` 为正确的后端地址。

### 使用其他 AI 模型

编辑 `.env` 文件，添加其他 API 密钥：

```env
# OpenAI
OPENAI_API_KEY=你的密钥

# 或其他兼容 OpenAI 的服务
```

然后在前端界面选择对应的模型。

## 💾 数据隐私

- 所有数据仅保存在你的电脑本地
- 不会上传到任何第三方服务器（除了 AI API 调用）
- 历史记录存储在浏览器 localStorage 中
- 可以随时清空历史记录

## 📝 开发说明

如果你想修改或扩展功能：

```bash
# 项目结构
backend/
├── src/
│   ├── controllers/     # 控制器
│   ├── services/        # 业务逻辑
│   ├── routes/          # API路由
│   └── server.js        # 服务器入口
frontend/
└── src/
    ├── components/      # 组件
    ├── services/        # API调用
    ├── utils/           # 工具函数
    └── app.js           # 应用入口
```

## 🎉 开始使用吧！

好了，现在你可以开始使用本小姐为你打造的降重工具了！

记住：
- ✅ 合理使用工具，提高效率
- ✅ 学习改写技巧，提升写作能力
- ✅ 保留原文思想，只是改变表达
- ❌ 不要直接抄袭，要理解消化

**哼，祝你的论文顺利通过查重！** (￣▽￣)／

---

_有任何问题欢迎查看 README.md 或提交 Issue！_
